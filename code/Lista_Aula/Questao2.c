#include <stdio.h>
#include <stdlib.h>

float soma(float som,float num)
{
  som = num+num;

  return som;
}

int main() {
  // Entrada de dados
  float som; // Soma
  float num, den; // Numerador, Denominador

  printf("\nDigite um numero positivo e inteiro: ");
  scanf("%f", &num);

  return = float soma(float som,float num,float den);



  printf("\nSoma = %.2f\n", som);

  return 0;
}
